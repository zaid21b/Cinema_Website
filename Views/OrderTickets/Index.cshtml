@model IEnumerable<Cinema_Website.Models.OrderTicket>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ticket)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Order)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Ticket.TicketId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Order.OrederId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.OrderTicketId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OrderTicketId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OrderTicketId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
