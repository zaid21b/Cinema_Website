@model Cinema_Website.Models.Seat

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Seat</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="SeatId" />
            <div class="form-group">
                <label asp-for="SeatT" class="control-label"></label>
                <select asp-for="SeatT" asp-items="@Html.GetEnumSelectList<Cinema_Website.Models.Seat.SeatType>()">" class="form-control"></select>
                <span asp-validation-for="SeatT" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Screen" class="control-label"></label>
                <select asp-for="Screen" asp-items="@Html.GetEnumSelectList<Cinema_Website.Models.Seat.ScreenType>()">" class="form-control"></select>
                <span asp-validation-for="Screen" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="HallId" class="control-label"></label>
                <select asp-for="HallId" class="form-control" asp-items="ViewBag.HallId"></select>
                <span asp-validation-for="HallId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
